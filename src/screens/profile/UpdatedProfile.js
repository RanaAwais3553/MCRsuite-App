import React from "react";
import { View, Text } from "react-native";
import Profile from "../../../src/common/component/organisms/profile/UpdatedProfile";

export default function UpdatedProfile({ navigation }) {
  return <Profile navigation={navigation} />;
}
