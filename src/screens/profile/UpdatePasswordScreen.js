import React from "react";
import { View, Text } from "react-native";
import UpdatePassword from "../../../src/common/component/organisms/profile/UpdatePassword";

export default function UpdatedPasswordScreen({ navigation }) {
  return <UpdatePassword navigation={navigation} />;
}
